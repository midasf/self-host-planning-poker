# Pin the lint rule set to ruff's documented defaults so CI is deterministic
# across ruff versions. This matches the bar the original Flask backend was
# linted against (E/F only).
[lint]
select = ["E4", "E7", "E9", "F"]
